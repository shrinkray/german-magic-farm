import type { PageServerLoad } from './$types';

export const load: PageServerLoad = async () => {
  // TODO: Add data fetching for shows and competitions when needed
  return {
    // Add any data you want to pass to the page here
  };
}; 